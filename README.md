## Member 1 Scope

### Features
- Facilities catalogue
- Resource management endpoints
- Booking workflow
- Booking conflict checking

### Branch
- `feature/member1-facilities-booking`

### This Folder Contains
- `backend/...` files for member 1 scope
- `frontend/...` files for member 1 scope
- `FILES.txt` full checklist of copied files

### Steps
1. Open project root and create branch:
	- `git checkout -b feature/member1-facilities-booking`
2. Copy this folder's `backend` and `frontend` contents into project root.
3. Verify with `FILES.txt`.
4. Commit and push:
	- `git add .`
	- `git commit -m "Member 1: facilities, resources, booking workflow"`
	- `git push -u origin feature/member1-facilities-booking`
5. Create PR to `main`.
